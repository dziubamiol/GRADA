import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            dark: '#1769aa',
            main: '#2196f3',
            light: '#4dabf5'
        },
        secondary: {
            dark: '#007bb2',
            main: '#00e5ff'
        }
    },
});

export default theme;
